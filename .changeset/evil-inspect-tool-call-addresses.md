---
"@rejelly/evil-jelly": minor
---

Add Session-global `TCn` addresses to Tool Call inspection, show them in Turn waterfalls and Tool reports, and allow `evil inspect --tools` to select a call by either its `TCn` address or persisted ToolCall ID.
