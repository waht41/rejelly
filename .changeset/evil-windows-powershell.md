---
"@rejelly/evil-jelly": patch
---

Run Windows shell commands through an explicit PowerShell invocation instead of `cmd.exe`, preserving multiline arguments, UTF-8 output, and native process exit codes. Update tool guidance to describe the PowerShell syntax expected on Windows.
