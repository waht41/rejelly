---
"@rejelly/core": minor
---

Allow `recordToolUsage` to attribute model token usage to LLM-backed tools through `modelUsages`. Include those tokens and token details in aggregate budgets while retaining the tool as the billing and usage item.
