---
"@rejelly/evil-jelly": patch
---

Make Escape dismiss the active `/`, `@`, or `$` composer picker before clearing the draft. The unfinished trigger text is preserved, and the dismissed picker stays closed until the user leaves that trigger and starts a new one.
