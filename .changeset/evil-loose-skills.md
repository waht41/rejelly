---
"@rejelly/evil-jelly": minor
---

Add process-lifetime loose Skills from fixed user and project roots, with bounded catalog and resource tools, layered enablement settings, and explicit per-input selection through the interactive `$` picker. Selected Skills are stored as atomic prompt tokens with dedicated display and submission forms, while logical-column cursor movement keeps multiline editing predictable. Skill and file pickers can now be triggered from any input line, and Skill rows use a compact name, type, and description layout that adds the source only when duplicate names need disambiguation.
