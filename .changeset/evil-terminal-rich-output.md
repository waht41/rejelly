---
"@rejelly/evil-jelly": minor
---

Refresh the terminal conversation presentation with a lighter prompt boundary, vertical blockquote rules, Codex-style Markdown tables, and language-aware syntax highlighting that loads only when code needs it. Static resume history now waits for highlighting readiness, preventing one response from being rendered with a mixture of highlighted and plain code.

Present edit operations as compact unified diffs with consistent colors, display-width-aware line layout, normalized Windows paths, and marked continuations for wrapped lines. Successful tool observations, including applied edit diffs and complete results, are now recorded in the session journal and restored into resumed transcripts so the useful intermediate tool history is not lost.
