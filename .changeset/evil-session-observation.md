---
"@rejelly/evil-jelly": minor
---

Persist `model_call_completed` and `tool_call_completed` session facts with prompt-composition metrics, token usage, per-attempt transport retry facts, and owner-reported tool outcomes, so a finished session journal can be inspected call by call.
