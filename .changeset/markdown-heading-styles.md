---
"@rejelly/evil-jelly": patch
---

Give each markdown heading level its own terminal marker so section titles remain visually distinct from list items: a rule under level one, followed by `▌`, `●`, and `·` markers for deeper levels.
