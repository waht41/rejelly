---
"@rejelly/evil-jelly": patch
---

Resume compacted sessions without rendering the internal compaction bridge as a user message. Retained historical user input is displayed without its model-only wrapper, and session turn counts and fallback titles now exclude compaction bookkeeping.
