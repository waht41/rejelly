---
"@rejelly/evil-jelly": patch
---

Assign stable, session-level ordinals to pasted images so image references remain unique and sequential across turns and resumed sessions, while upgrading older session records during replay without rewriting stored events.
