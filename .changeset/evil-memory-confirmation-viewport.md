---
"@rejelly/evil-jelly": patch
---

Keep persistent-memory confirmation previews inside a bounded, scrollable viewport so large before/after payloads do not overflow the terminal and leave duplicate frames in scrollback.
