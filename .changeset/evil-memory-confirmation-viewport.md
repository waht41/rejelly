---
"@rejelly/evil-jelly": patch
---

Keep persistent-memory confirmation previews inside a bounded, J/K-scrollable viewport so large before/after payloads do not overflow the terminal and leave duplicate frames in scrollback. Return explicit accepted-and-saved or rejected-by-user outcomes so the agent does not treat a finished confirmation as still pending.
