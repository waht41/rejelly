---
"@rejelly/evil-jelly": minor
---

Make `grep` searches literal by default, add an explicit regex mode, validate regular expressions before selecting a backend, and report native search failures without misclassifying parameter or process errors as fallback availability problems.
