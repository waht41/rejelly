---
"@rejelly/evil-jelly": patch
---

Let the `@` file picker progressively complete paths inside an exact ignored directory without exposing ignored workspace-wide matches. Exact ignored files can now be selected and attached, while sensitive and protected paths remain hidden.
