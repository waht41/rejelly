---
"@rejelly/evil-jelly": patch
---

Allow the `grep` tool's `directory` parameter to accept a concrete file path as a single-file search fallback, while preserving directory scans and optional file-pattern filtering.
