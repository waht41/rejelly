---
"@rejelly/devtool": patch
---

Display model tokens, token details, provider, and model attribution for LLM-backed tool usage in the budget inspector. Preserve the same nested attribution in SQLite trace summaries without adding the tokens to trace totals twice.
