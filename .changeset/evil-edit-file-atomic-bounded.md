---
"@rejelly/evil-jelly": minor
---

Make `edit_file` apply edits atomically per file, report every invalid block without blocking conflict-free files, and support bounded replacements between unique start and end anchors.
