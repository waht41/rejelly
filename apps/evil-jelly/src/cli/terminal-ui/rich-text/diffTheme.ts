/** Shared semantic colors for rendered diffs and Markdown `diff` code fences. */
export const DIFF_COLORS = {
  addition: "#a6e3a1",
  deletion: "#f38ba8",
  hunk: "#89dceb",
  heading: "#89dceb",
  meta: "#6c7086",
} as const;
